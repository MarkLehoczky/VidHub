<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="VidHub.WinUI.UserControls.Dialogs.TagsUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VidHub.WinUI.UserControls.Dialogs"
    xmlns:viewmodel="using:VidHub.ViewModels.Dialogs"
    xmlns:converter="using:VidHub.WinUI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.DataContext>
        <viewmodel:TagViewModel />
    </UserControl.DataContext>

    <UserControl.Resources>
        <converter:ColorConverter x:Key="ColorConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Button Command="{Binding AddTagCommand}" Content="&#xe710;" FontFamily="Segoe Fluent Icons" Margin="10" Grid.Row="0" />

        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Hidden" Grid.Row="1">
            <ItemsControl x:Name="tagcollection" ItemsSource="{Binding Tags}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>

                        <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" CornerRadius="{ThemeResource ControlCornerRadius}" HorizontalAlignment="Stretch" Padding="10" Margin="0 5">
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition />
                            </Grid.RowDefinitions>

                            <Grid HorizontalAlignment="Stretch" Grid.Row="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Button Command="{Binding ElementName=tagcollection, Path=DataContext.RemoveTagCommand}" CommandParameter="{Binding}" Content="&#xe74d;" FontFamily="Segoe Fluent Icons" Margin="0 0 5 0" Grid.Column="0" />
                                <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" MaxLength="10" PlaceholderText="Tag text" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="5 0 5 0" Grid.Column="1" />
                                <Border Background="{Binding Color, Converter={StaticResource ColorConverter}}" BorderThickness="0" CornerRadius="10" Margin="5 0 5 0" HorizontalAlignment="Left" VerticalAlignment="Center" Grid.Column="2" >
                                    <TextBlock Text="{Binding Name}" FontFamily="Consolas" Margin="6 2" />
                                </Border>
                                <Button Content="{Binding ColorPickerIcon}" Command="{Binding ElementName=tagcollection, Path=DataContext.ChangeColorPickerStateCommand}" CommandParameter="{Binding}" FontFamily="Segoe Fluent Icons" VerticalAlignment="Center" Margin="5 0 0 0" Grid.Column="3" />
                            </Grid>
                            <ColorPicker Visibility="{Binding ColorPickerOpen}" Color="{Binding Color, Mode=TwoWay}" IsAlphaEnabled="True" IsAlphaTextInputVisible="False" IsColorChannelTextInputVisible="False" IsHexInputVisible="False" Margin="0 10 0 0" Grid.Row="1" />
                        </Grid>
                        
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

    </Grid>

</UserControl>
