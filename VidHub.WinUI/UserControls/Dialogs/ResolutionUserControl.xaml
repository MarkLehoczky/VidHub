<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="VidHub.WinUI.UserControls.Dialogs.ResolutionUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VidHub.WinUI.UserControls.Dialogs"
    xmlns:viewmodel="using:VidHub.ViewModels.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.DataContext>
        <viewmodel:ResolutionViewModel />
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Button Command="{Binding AddResolutionCommand}" Content="&#xe710;" FontFamily="Segoe Fluent Icons" Margin="10" Grid.Row="0" />

        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Hidden" Grid.Row="1">
            <ItemsControl x:Name="tagcollection" ItemsSource="{Binding Resolutions}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>

                        <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" CornerRadius="{ThemeResource ControlCornerRadius}" HorizontalAlignment="Stretch" Padding="10" Margin="0 5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Button Command="{Binding ElementName=tagcollection, Path=DataContext.RemoveResolutionCommand}" CommandParameter="{Binding}" Content="&#xe74d;" FontFamily="Segoe Fluent Icons" VerticalAlignment="Stretch" Grid.Column="0" />
                            <TextBox Text="{Binding Name}" Header="Name" PlaceholderText="e.g. Full HD" Margin="10 0 20 0" Grid.Column="1" />
                            <NumberBox Value="{Binding Width}" Header="Width" PlaceholderText="e.g. 1920" Minimum="0" SmallChange="1" LargeChange="100" Grid.Column="2" />
                            <TextBlock Text="×" Margin="4 8" VerticalAlignment="Bottom" Grid.Column="3" />
                            <NumberBox Value="{Binding Height}" Header="Height" PlaceholderText="e.g. 1080" Minimum="0" SmallChange="1" LargeChange="100" Grid.Column="4" />
                        </Grid>

                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

    </Grid>
</UserControl>
