<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="VidHub.WinUI.UserControls.Dialogs.ModifyTagsUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:local="using:VidHub.WinUI.UserControls.Dialogs"
    xmlns:viewmodel="using:VidHub.ViewModels.Dialogs"
    xmlns:converter="using:VidHub.WinUI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.DataContext>
        <viewmodel:ModifyTagsViewModel />
    </UserControl.DataContext>

    <UserControl.Resources>
        <converter:ColorConverter x:Key="ColorConverter" />
    </UserControl.Resources>

    <ItemsRepeater ItemsSource="{Binding Tags}" HorizontalAlignment="Center">
        <ItemsRepeater.Layout>
            <controls:WrapLayout Orientation="Horizontal" HorizontalSpacing="10" VerticalSpacing="10" />
        </ItemsRepeater.Layout>
        <ItemsRepeater.ItemTemplate>
            <DataTemplate>
                <ToggleButton IsChecked="{Binding VideoContains}" Checked="AddTagToVideo" Unchecked="RemoveTagFromVideo" HorizontalAlignment="Stretch" HorizontalContentAlignment="Center">
                    <Border Background="{Binding Color, Converter={StaticResource ColorConverter}}" BorderThickness="0" CornerRadius="10" Margin="5 0 5 0" HorizontalAlignment="Left" VerticalAlignment="Center" Grid.Column="2" >
                        <TextBlock Text="{Binding Name}" FontFamily="Consolas" Margin="6 2" />
                    </Border>
                </ToggleButton>
            </DataTemplate>
        </ItemsRepeater.ItemTemplate>
    </ItemsRepeater>
</UserControl>
