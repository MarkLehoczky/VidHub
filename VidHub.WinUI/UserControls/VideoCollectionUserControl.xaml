<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="VidHub.WinUI.UserControls.VideoCollectionUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VidHub.WinUI.UserControls"
    xmlns:converter="using:VidHub.WinUI.Converters"
    xmlns:viewmodel="using:VidHub.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <converter:DateFormatConverter x:Key="FormatDate" />
        <converter:DurationFormatConverter x:Key="FormatDuration" />
    </UserControl.Resources>

    <UserControl.DataContext>
        <viewmodel:VideoCollectionViewModel />
    </UserControl.DataContext>

    <Grid AllowDrop="True" DragOver="DragOverItems" Drop="DropItems">
        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Videos}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="10" MaxWidth="450">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            
                            <Border Margin="0" Padding="0" BorderThickness="0" CornerRadius="8" HorizontalAlignment="Center" VerticalAlignment="Bottom" Grid.Row="0">
                                <Grid>
                                    <Image Source="{Binding ThumbnailPath}" MaxHeight="425" Stretch="Uniform" />
                                    <Border Background="Black" Opacity="0.8" BorderThickness="0" CornerRadius="8" Margin="4" HorizontalAlignment="Right" VerticalAlignment="Bottom" >
                                        <TextBlock Text="{Binding Duration, Converter={StaticResource FormatDuration}}" Margin="6 2" />
                                    </Border>
                                </Grid>
                            </Border>
                            
                            <TextBlock Text="{Binding Title}" FontWeight="Bold" TextTrimming="CharacterEllipsis" ToolTipService.ToolTip="{Binding Title}" Margin="10 5 10 0" Grid.Row="1" />
                            <TextBlock Text="{Binding Date, Converter={StaticResource FormatDate}}" FontStyle="Italic" Margin="10 0 10 0" HorizontalAlignment="Left" VerticalAlignment="Center" Grid.Row="2" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>

</UserControl>
