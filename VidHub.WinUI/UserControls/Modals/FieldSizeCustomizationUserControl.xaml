<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="VidHub.WinUI.UserControls.Modals.FieldSizeCustomizationUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VidHub.WinUI.UserControls.Modals"
    xmlns:viewmodel="using:VidHub.ViewModels.Modals"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.DataContext>
        <viewmodel:FieldSizeCustomizationViewModel />
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>


        <TextBlock Text="Width" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="5" Grid.Row="0" Grid.Column="0"  />
        <TextBlock Text="Height" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="5" Grid.Row="1" Grid.Column="0"  />

        <NumberBox Value="{Binding FieldWidth, Mode=TwoWay}" Minimum="1" SmallChange="10" SpinButtonPlacementMode="Inline" ValidationMode="InvalidInputOverwritten" PlaceholderText="E.g. 480" Width="175" Margin="5" Grid.Row="0" Grid.Column="1"  />
        <NumberBox Value="{Binding FieldHeight, Mode=TwoWay}" Minimum="1" SmallChange="10" SpinButtonPlacementMode="Inline" ValidationMode="InvalidInputOverwritten" PlaceholderText="E.g. 270" Width="175" Margin="5" Grid.Row="1" Grid.Column="1"  />
    </Grid>
</UserControl>
