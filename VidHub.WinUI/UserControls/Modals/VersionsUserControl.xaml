<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="VidHub.WinUI.UserControls.Modals.VersionsUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VidHub.WinUI.UserControls.Modals"
    xmlns:viewmodel="using:VidHub.ViewModels.Modals"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.DataContext>
        <viewmodel:VersionsViewModel />
    </UserControl.DataContext>

    <ScrollViewer Padding="0 0 5 0">
        <ItemsControl ItemsSource="{Binding Versions}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" CornerRadius="{ThemeResource ControlCornerRadius}" Padding="10" Margin="0 0 0 30">
                        <TextBlock Text="{Binding Version}" FontWeight="Bold" HorizontalAlignment="Center" FontSize="24"/>

                        <StackPanel Visibility="{Binding HasNewFeatures}">
                            <TextBlock Text="✨ New Features" FontWeight="Bold" FontSize="16" Margin="0 0 10 0"/>
                            <ItemsControl ItemsSource="{Binding NewFeatures}" Margin="0 5 0 0">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="•  " Margin="0 0 0 5">
                                        <Run Text="{Binding}" />
                                        </TextBlock>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>

                        <StackPanel Visibility="{Binding HasBugFixes}">
                            <TextBlock Text="🐞 Bug Fixes" FontWeight="Bold" FontSize="16" Margin="0 10 0 0"/>
                            <ItemsControl ItemsSource="{Binding BugFixes}" Margin="0 10 0 0">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="•  " Margin="0 0 0 5">
                                        <Run Text="{Binding}" />
                                        </TextBlock>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>

                        <StackPanel Visibility="{Binding HasInternalChanges}">
                            <TextBlock Text="🛠️ Internal Changes" FontWeight="Bold" FontSize="16" Margin="0 10 0 0"/>
                            <ItemsControl ItemsSource="{Binding InternalChanges}" Margin="0 10 0 0">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="•  " Margin="0 0 0 5">
                                        <Run Text="{Binding}" />
                                        </TextBlock>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>

                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</UserControl>
